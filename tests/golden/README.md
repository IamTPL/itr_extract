# Golden fixtures

- Test: `tests/test_golden_cases.py` — chạy chung trong `venv/bin/python -m pytest`, offline.
- Fixtures: `cases/<tên>/` — chứa dữ liệu client → **gitignored, không commit**.
- Tạo lại fixture: `capture_golden.py` (gọi Gemini thật).
- Toàn bộ hướng dẫn: **`docs/TESTING.md`**.
